#!/bin/sh

# This is the script that i3 runs to either start tmux in
# the dropdown terminal or log into a previous session.
tmux attach -t pyradio || (cat ~/.cache/wal/sequences &) ; tmux new -s pyradio pyradio
